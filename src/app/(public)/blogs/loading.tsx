import Loading from '@/components/ui/Loading';

const BlogLoadingPage = () => {
    return (
        <div className='py-32'>
            <Loading/>
        </div>
    );
};

export default BlogLoadingPage;